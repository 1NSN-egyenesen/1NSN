# Configuration for mklive-stretch, modify as desired
# NOTE: Leave every commented (#) line commented as it is
# See at the bottom commented out for info

### Start configuration
DE_CONFIG="Mate"
# Be careful with removing from this section (mostly essential)  
BASE_INSTALL="acl acpi acpi-support-base acpid aespipe anacron apt-rdepends apt-utils bc bsdmainutils busybox bzip2 console-setup console-setup-linux cpio cpufrequtils cryptsetup-bin curlftpfs dash dbus-x11 desktop-file-utils dhex dosfstools e2fsck-static eject fakeroot file flac gettext-base gnupg gnupg-agent gpart gtk-update-icon-cache gtk2-engines-pixbuf gtklp gzip hsetroot ifupdown init initramfs-tools initramfs-tools-core initscripts insserv iproute2 iptables iputils-ping isolinux kbd keyboard-configuration keyboards-rg klibc-utils kmod lame less linux-base linux-user-chroot live-boot live-boot-initramfs-tools live-tools lm-sensors logrotate lsof makedev mime-support nano ncurses-term net-tools netbase ntfs-3g openssl parted patch pciutils pcmciautils perl perl-openssl-defaults pinentry-curses pm-utils policykit-1 powermgmt-base ppp pppoe pppoeconf procps psmisc pv rcconf readline-common rfkill rsync rsyslog scrot sgml-base startpar sudo sysv-rc sysvinit-core time traceroute ucf unzip usbutils util-linux-locales vim-common vim-tiny whiptail wireless-tools wmctrl wvdial x11-xserver-utils xauth xbacklight xdg-utils xdg-user-dirs xdotool xinit xinput xkb-data xml-core xorriso xserver-xorg xserver-xorg-core xserver-xorg-input-evdev xserver-xorg-input-mouse xserver-xorg-input-synaptics xserver-xorg-video-ati xserver-xorg-video-cirrus xserver-xorg-video-fbdev xserver-xorg-video-intel xserver-xorg-video-mga xserver-xorg-video-nouveau xserver-xorg-video-openchrome xserver-xorg-video-radeon xserver-xorg-video-vesa xterm xxd xz-utils zip curl fdisk mtools"

# Base Dog Packages, recommended to keep:
# yad gtkdialog obshutdown pup-volume-monitor peasywifi
BASE_DOG_APPS_INSTALL="yad gtkdialog obshutdown pup-volume-monitor peasywifi chpupsocket edit-sfs-pcmanfm filemnt-pcmanfm remaster-scripts quick-remaster apt2sfs sfsload fixdepinstall greybird-theme-dd-stretch makedebpackage"

BASE_APPS_INSTALL="minidlna gsmartcontrol gmtp conky gdmap gparted geany roxterm-gtk2 pcmanfm mtpaint pack-it uextract alsa-utils"

DESK_APPS_INSTALL="mate-core mate-tweak"
  
EXTRA_DOG_APPS_INSTALL="upgrade-kernel debdoginstallscripts debdogmountscripts"

REM_AUTO_INST="TRUE"

FORCE32="FALSE"
KEEPLOCALES="TRUE"
KEEPMANDOC="FALSE"
SYSTEMD="FALSE"
RUNXTERM="TRUE"
LBINITRD="FALSE"
ISOUEFI="FALSE"
NMU="FALSE"  # set to TRUE to create a no-merged-usr filesystem
### End configuration

# A selection of firmware packages available (mostly for wireless):
# atmel-firmware firmware-realtek bluez-firmware firmware-atheros firmware-linux-free firmware-linux-nonfree firmware-netxen firmware-ti-connectivity firmware-b43legacy-installer firmware-iwlwifi firmware-ipw2x00 firmware-libertas firmware-intelwimax firmware-brcm80211 firmware-b43-installer firmware-qlogic firmware-bnx2 firmware-misc-nonfree firmware-bnx2x firmware-zd1211 
# Some extra dog applications:
# debdoginstallscripts dogradio youtube-get2 straw-viewer peasyfwmon gifondesktop upgrade-kernel conkyclock redshiftgui redshift-gui-lite mpv peasymount peasyscale peasyxorburn peasyglue
# Choice of Desktop:
# Default is openbox with pcmanmfm providing the desktop, lxpanel, etc..
# To change, replace what is in the DESK_APPS_INSTALL field with for example:
# mate-core (for MATE), xfce4 (for XFCE), lxde (for LXDE)
# No guarantee that all work as expected, might require some fixing
# Remove automatically installed packages:
# During installing all the packages, some are installed that you might not need, e.g.
# xserver-xorg-video-vmware, REM_AUTO_INST=TRUE will uninstall it.
# (possibly (large) library libllvm3.9 will then be autoremoved, depending on your other package choices)
# To force 32 bit build on a 64 bit OS set FORCE32=TRUE
# Locales: To keep locale files, set to KEEP_LOCALES=yes on top of the script
